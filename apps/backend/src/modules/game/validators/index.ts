'use strict';
/**
 * Game Module — Validators barrel export
 */
const authValidator   = require('./authValidator');
const walletValidator = require('./walletValidator');

module.exports = { authValidator, walletValidator };
