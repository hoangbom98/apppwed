{
  "name": "@lkvip/scripts-utils",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Shared script utilities — logger (chalk) and env loader (dotenv) for LKVIP scripts",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "dotenv": "^16.6.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "~6.0.2"
  }
}
