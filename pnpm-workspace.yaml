packages:
  # ── Core platform apps (hub, game, trading, dating, sports, admin-dashboard,
  #    backend, mobile, academy, banking, invest, lkvip-store, lkvipgroup-portal)
  - "apps/*"
  - "packages/*"

  # ── External projects ─────────────────────────────────────────────────────
  # Các project trong apps/external/ là ISOLATED — không phải phần của LKVIP
  # platform build. Chúng KHÔNG được hoist vào workspace này.
  # Xem apps/external/README.md để biết thêm chi tiết.
