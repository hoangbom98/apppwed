'use strict';
/**
 * Trade Module — Validators barrel export
 */
const authValidator   = require('./authValidator');
const tradeValidator  = require('./tradeValidator');

module.exports = { authValidator, tradeValidator };
