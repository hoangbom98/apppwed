// game/src/utils/dinhDang.ts — formatters re-exported from shared-ui
// All format functions are now canonical in @ui (shared-ui/utils/formatters.js)
export {
  formatVND,
  formatNumber,
  formatDate,
  formatTime,
  formatDateTime,
  formatRelativeTime,
  relativeTime,
  formatDuration,
  formatScore,
  formatDistance,
  formatAge,
  truncate,
  clamp,
  formatCoins,
  formatDecimal,
  fmtPct,
  fmtVol,
} from '@ui';
