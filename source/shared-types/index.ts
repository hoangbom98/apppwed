/**
 * @lkvip/types — barrel re-export
 *
 * This file is intentionally minimal.
 * All type definitions live in src/index.ts (single source of truth).
 *
 * Import:
 *   import type { IUser, ProjectId } from '@lkvip/types';
 */
export * from './src/index';
